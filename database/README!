В файле models.py лежат объекты таблиц, можете исправлять,
если это нужно, но предупредите меня об этом.
Докстринги к объектам имеются.

В migrate.py написана функция запускающая миграцию БД,
на вход принимае список изменений (объекты созданные с
помощью migrate.*).
